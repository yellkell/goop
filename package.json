{
  "name": "goop",
  "version": "0.1.0",
  "description": "GOOP — a passthrough XR boxing bout against a man-sized gel creature: knock satisfying lumps out of the glob and watch it reform, dodge its punches when it pulls itself into a fighting shape. Built on the Immersive Web SDK.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@iwsdk/core": "0.4.2",
    "three": "0.184.0"
  },
  "devDependencies": {
    "@iwsdk/vite-plugin-dev": "0.4.2",
    "@types/three": "0.184.0",
    "playwright": "^1.61.1",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
